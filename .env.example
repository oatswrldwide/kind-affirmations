# Supabase Configuration (not required for Express backend)
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# Express Backend API URL (optional - defaults to localhost:3001 in development)
# For production, set this to your Railway backend URL
# VITE_API_URL=https://your-app.up.railway.app/api/generate-affirmation

# Note: OPENROUTER_API_KEY is set in backend/.env
# See backend/.env.example for backend environment variables
